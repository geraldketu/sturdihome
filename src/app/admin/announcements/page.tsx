import { requirePageAccess } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { Card } from "@/components/ui";
import { saveAnnouncementAction } from "@/lib/actions/content-actions";
export default async function AdminAnnouncementsPage(){await requirePageAccess("/admin/announcements");const items=await prisma.siteAnnouncement.findMany({orderBy:{createdAt:"desc"}});return <div className="space-y-6"><h1 className="text-2xl font-bold text-brand-dark">Announcements</h1><Card><form action={saveAnnouncementAction} className="space-y-3"><input name="title" required placeholder="Announcement title" className="w-full rounded border p-2"/><textarea name="body" required placeholder="Announcement text" className="w-full rounded border p-2"/><div className="grid gap-3 sm:grid-cols-3"><input name="ctaLabel" placeholder="CTA label" className="rounded border p-2"/><input name="ctaHref" placeholder="CTA link" className="rounded border p-2"/><input name="sortOrder" type="number" placeholder="Order" className="rounded border p-2"/></div><label className="flex gap-2"><input name="active" type="checkbox"/> Show now</label><button className="rounded bg-brand px-4 py-2 font-semibold text-white">Create Announcement</button></form></Card>{items.map(item=><Card key={item.id}><p className="font-semibold">{item.title}</p><p className="mt-1 text-sm text-gray-600">{item.body}</p><p className="mt-1 text-xs">{item.active?"Active":"Hidden"}</p></Card>)}</div>}
