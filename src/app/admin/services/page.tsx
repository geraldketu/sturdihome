import { requirePageAccess } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { Card } from "@/components/ui";
import { saveServiceCategoryAction } from "@/lib/actions/content-actions";
export default async function AdminServicesPage(){await requirePageAccess("/admin/services");const services=await prisma.serviceCategory.findMany({orderBy:[{sortOrder:"asc"},{label:"asc"}]});return <div className="space-y-6"><h1 className="text-2xl font-bold text-brand-dark">Manage Services</h1><Card><form action={saveServiceCategoryAction} className="grid gap-3 sm:grid-cols-2"><input name="label" required placeholder="Service name" className="rounded border p-2"/><input name="slug" placeholder="slug" className="rounded border p-2"/><input name="description" placeholder="Description" className="rounded border p-2"/><input name="imageUrl" placeholder="Image URL" className="rounded border p-2"/><input name="sortOrder" type="number" placeholder="Display order" className="rounded border p-2"/><label className="flex gap-2 p-2"><input name="active" type="checkbox" defaultChecked/> Active</label><button className="rounded bg-brand px-4 py-2 font-semibold text-white sm:col-span-2">Add Service</button></form></Card><div className="space-y-3">{services.map(service=><Card key={service.id}><div className="flex items-center justify-between"><div><p className="font-semibold">{service.label}</p><p className="text-sm text-gray-600">{service.description}</p></div><span className="text-xs">{service.active?"Active":"Hidden"}</span></div></Card>)}</div></div>}
