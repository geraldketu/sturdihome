"use client";
import { useActionState } from "react";
import { acceptMemberStandingAction } from "@/lib/actions/account-actions";
import { FormError, SubmitButton } from "@/components/ui";
export default function StandingAgreementForm() { const [state, action] = useActionState(acceptMemberStandingAction, undefined); return <form action={action} className="mt-5 space-y-4"><FormError message={state?.error} /><label className="flex items-start gap-3 text-sm"><input type="checkbox" name="accepted" value="yes" required className="mt-1" />I have read and agree to this Member Account Standing and Platform Access Agreement.</label><label className="flex items-start gap-3 text-sm"><input type="checkbox" name="electronicConsent" value="yes" required className="mt-1" />I consent to electronic records and electronic signatures.</label><label className="block text-sm font-medium text-gray-700">Full Legal Name<input name="fullLegalName" required maxLength={200} className="mt-1 w-full rounded-md border border-gray-300 px-3 py-2" /></label><label className="block text-sm font-medium text-gray-700">Electronic Signature<input name="electronicSignature" required maxLength={200} placeholder="Type your full legal name" className="mt-1 w-full rounded-md border border-gray-300 px-3 py-2" /></label><SubmitButton pendingText="Signing...">AGREE &amp; SIGN</SubmitButton></form>; }
